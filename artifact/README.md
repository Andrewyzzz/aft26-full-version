# Artifact

This directory contains the lightweight review-time artifact.

- `scripts/`: pipeline scripts (data collection, metrics, mechanism, counterfactual, validation)
- `tables/`: aggregated CSV outputs corresponding to paper tables

See the top-level README for a full description of each file.
